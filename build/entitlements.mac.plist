<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- JIT compilation for performance -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (needed for some native modules) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dynamic linker environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation (needed for Homebrew database binaries) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client access (database connections, HTTP requests) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Network server access (MCP server, database servers) -->
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- User-selected file access (database files, custom images) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Downloads folder access -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
    <!-- Apple Events automation (system preferences, helper service) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <!-- Keychain access for secure storage (safeStorage API) -->
    <key>keychain-access-groups</key>
    <array>
      <string>$(AppIdentifierPrefix)com.liquidb.app</string>
    </array>
    <!-- Mach lookup exceptions for system services -->
    <key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
    <array>
      <string>com.apple.coreservices.launchservicesd</string>
    </array>
  </dict>
</plist>

